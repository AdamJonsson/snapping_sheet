name: snapping_sheet_example
description: Snapping Sheet Example

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  snapping_sheet: 3.0.0-nullsafety.0

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  snapping_sheet: 
    path: ../


flutter:
  uses-material-design: true